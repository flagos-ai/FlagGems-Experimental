# Copyright 2026, The FlagOS Contributors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
# implied. See the License for the specific language governing permissions
# and limitations under the License.
#
# Generated by KernelGen: https://github.com/flagos-ai/KernelGen

import pytest
import torch
from _pytest.mark.structures import Mark, MarkDecorator

import flag_gems

from . import base

# ``_make_per_tensor_quantized_tensor`` starts with an underscore; pytest
# refuses marker attribute access for such names, so register both markers on
# the MarkGenerator directly.
setattr(
    pytest.mark,
    "_make_per_tensor_quantized_tensor",
    MarkDecorator(
        Mark("_make_per_tensor_quantized_tensor", (), {}, _ispytest=True),
        _ispytest=True,
    ),
)
setattr(
    pytest.mark,
    "_make_per_tensor_quantized_tensor_out",
    MarkDecorator(
        Mark("_make_per_tensor_quantized_tensor_out", (), {}, _ispytest=True),
        _ispytest=True,
    ),
)

# The op is a pure memory copy from an integer tensor into a quantized tensor's
# int representation. The only input dtypes it accepts are uint8, int8 and
# int32 (the three toQIntType sources), so the dtype sweep is those three
# instead of the usual float/int/bool sets -- a float input raises from the
# quantized allocation factory.
INPUT_DTYPES = [torch.uint8, torch.int8, torch.int32]

# Shapes: 1-D and ND, and sizes on both sides of the flat kernel's tuning
# switch (BLOCK 8192 with num_warps 4 below 2**21 elements, 16384 with 8 warps
# above) plus a strided layout case, so the benchmark covers every kernel
# branch the implementation selects at runtime.
MKPTQT_SHAPES = [(1024,), (4096,), (1024, 1024), (1024, 2048), (2048, 2048)]

_QDTYPE = {
    torch.uint8: torch.quint8,
    torch.int8: torch.qint8,
    torch.int32: torch.qint32,
}


def _int_input(shape, cur_dtype, device):
    """Integer input of ``cur_dtype`` on ``device`` from the dtype's full range."""
    info = torch.iinfo(cur_dtype)
    if cur_dtype == torch.int32:
        low = torch.randint(-32768, 32768, shape, dtype=torch.int32, device="cpu")
        return (low * 65536 + torch.randint(0, 65536, shape, dtype=torch.int32)).to(
            device
        )
    return torch.randint(info.min, info.max, shape, dtype=cur_dtype, device="cpu").to(
        device
    )


def _out_buffer(shape, cur_dtype, device):
    return torch._empty_affine_quantized(
        shape,
        dtype=_QDTYPE[cur_dtype],
        device=device,
        scale=1.0,
        zero_point=0,
    )


class MakePerTensorQuantizedTensorBenchmark(base.Benchmark):
    def set_shapes(self, shape_file_path=None):
        self.shapes = MKPTQT_SHAPES

    def get_input_iter(self, cur_dtype):
        # The op takes (Tensor self, float scale, int zero_point) for the
        # functional form and an extra keyword-only ``out`` for the .out form;
        # yield positional args and a kwargs dict as the harness expects.
        for shape in self.shapes:
            inp = _int_input(shape, cur_dtype, self.device)
            yield inp, 0.5, 3

    def get_gbps(self, args, latency=None):
        # Bandwidth of a pure copy: read + write of one element per byte.
        n_bytes = args[0].numel() * args[0].element_size()
        return 2 * n_bytes / (latency / 1e3) / 1e9

    def set_more_metrics(self):
        return ["gbps"]


class MakePerTensorQuantizedTensorOutBenchmark(MakePerTensorQuantizedTensorBenchmark):
    def get_input_iter(self, cur_dtype):
        for shape in self.shapes:
            inp = _int_input(shape, cur_dtype, self.device)
            yield inp, 0.5, 3, {"out": _out_buffer(shape, cur_dtype, self.device)}


@pytest.mark._make_per_tensor_quantized_tensor
def test_make_per_tensor_quantized_tensor():
    bench = MakePerTensorQuantizedTensorBenchmark(
        op_name="_make_per_tensor_quantized_tensor",
        torch_op=torch.ops.aten._make_per_tensor_quantized_tensor,
        gems_op=flag_gems._make_per_tensor_quantized_tensor,
        dtypes=INPUT_DTYPES,
    )
    bench.run()


@pytest.mark._make_per_tensor_quantized_tensor_out
def test_make_per_tensor_quantized_tensor_out():
    bench = MakePerTensorQuantizedTensorOutBenchmark(
        op_name="_make_per_tensor_quantized_tensor.out",
        torch_op=torch.ops.aten._make_per_tensor_quantized_tensor.out,
        gems_op=flag_gems._make_per_tensor_quantized_tensor_out,
        dtypes=INPUT_DTYPES,
    )
    bench.run()
