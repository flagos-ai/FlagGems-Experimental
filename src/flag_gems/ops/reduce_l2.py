# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging
import math

import torch
import triton
import triton.language as tl

from flag_gems.runtime import torch_device_fn
from flag_gems.utils import libentry
from flag_gems.utils import triton_lang_extension as tle

logger = logging.getLogger(__name__)


def _get_block_size(numel, dtype):
    block_size = triton.next_power_of_2(math.ceil(math.sqrt(numel)))
    if dtype is torch.float32 and numel >= 2**28:
        block_size = min(block_size, 4096)
    return block_size


@libentry()
@triton.jit
def reduce_l2_kernel_1(X, Mid, M, BLOCK_SIZE: tl.constexpr):
    pid = tle.program_id(0).to(tl.int64)
    offset = pid * BLOCK_SIZE + tl.arange(0, BLOCK_SIZE)
    X = X + offset
    Mid = Mid + pid
    mask = offset < M

    x = tl.load(X, mask=mask, other=0.0).to(tl.float32)
    mid = tl.sum(x * x)
    tl.store(Mid, mid)


@libentry()
@triton.jit
def reduce_l2_kernel_2(Mid, Out, MID_SIZE, BLOCK_MID: tl.constexpr):
    offset = tl.arange(0, BLOCK_MID)
    Mid = Mid + offset
    mask = offset < MID_SIZE
    mid = tl.load(Mid, mask=mask, other=0.0).to(tl.float32)
    out = tl.sqrt(tl.sum(mid))
    tl.store(Out, out)


def reduce_l2(x):
    logger.debug("GEMS REDUCE_L2")
    x = x.contiguous()
    M = x.numel()

    dtype = x.dtype
    if dtype not in [torch.float16, torch.float32, torch.bfloat16]:
        raise NotImplementedError(f"reduce_l2 not implemented for {dtype}")
    if M == 0:
        return torch.zeros([], dtype=dtype, device=x.device)

    BLOCK_SIZE = _get_block_size(M, dtype)
    MID_SIZE = triton.cdiv(M, BLOCK_SIZE)
    BLOCK_MID = triton.next_power_of_2(MID_SIZE)

    mid = torch.empty([MID_SIZE], dtype=torch.float32, device=x.device)
    out = torch.empty([], dtype=torch.float32, device=x.device)

    with torch_device_fn.device(x.device):
        reduce_l2_kernel_1[(MID_SIZE,)](x, mid, M, BLOCK_SIZE)
        reduce_l2_kernel_2[(1,)](mid, out, MID_SIZE, BLOCK_MID)

    return out.to(dtype)
