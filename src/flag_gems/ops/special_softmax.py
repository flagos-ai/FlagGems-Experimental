# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

from flag_gems.ops.softmax import softmax as core_softmax

logger = logging.getLogger(__name__)


def special_softmax(self, dim, dtype=None):
    logger.debug("GEMS SPECIAL_SOFTMAX")

    if dtype is not None:
        # Cast input to the specified dtype for computation
        self = self.to(dtype)

    result = core_softmax(self, dim)

    return result
