# Generated by KernelGen: https://github.com/flagos-ai/KernelGen
import logging

import triton
import triton.language as tl

from flag_gems.utils import pointwise_dynamic, tl_extra_shim

logger = logging.getLogger(__name__)

# Get y0 function from tl_extra_shim for cross-backend compatibility
_y0 = tl_extra_shim.y0


@pointwise_dynamic(promotion_methods=[(0, "DEFAULT")])
@triton.jit
def special_bessel_y0_func(x):
    # Compute in fp32 for accuracy, then cast back
    x_f32 = x.to(tl.float32)
    # y0 is only defined for x > 0
    # For x <= 0, return nan
    result = _y0(x_f32)
    return result.to(x.dtype)


def special_bessel_y0(A):
    logger.debug("GEMS SPECIAL_BESSEL_Y0")
    return special_bessel_y0_func(A)


def special_bessel_y0_out(A, *, out=None):
    logger.debug("GEMS SPECIAL_BESSEL_Y0_OUT")
    if out is None:
        return special_bessel_y0_func(A)
    special_bessel_y0_func(A, out0=out)
    return out
